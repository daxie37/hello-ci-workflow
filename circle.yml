# circle.yml
machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t hello-ci-workflow .

test:
  override:
    - ./node_modules/.bin/mocha
    - docker run -d -p 3000:3000 hello-ci-workflow; sleep 10
    - curl --retry 10 --retry-delay 5 -v 192.168.99.100:3000
